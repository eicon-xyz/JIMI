1. **项目目录结构**：
   my_desktop_assistant/
   │
   ├── main.py                 # 应用入口
   ├── config.py               # 配置文件
   │
   ├── ui/                     # 【UI 视图层】
   │   ├── __init__.py
   │   ├── main_widget.py      # 👈 任务 1：需要内嵌 HTML 的桌面主挂件
   │   ├── overlay_anno.py     # 【已完成】纯 Python 原生实现的全屏透明、鼠标穿透红框标注层
   │   │
   │   └── web/                # 📂 存放我的前端静态资源
   │       └── index.html      # 👈 任务 2：即我上传的这门 2500 行 HTML
   │
   └── core/                   # 【感知核心层】
       └── ui_parser.py        # 后台 AI 线程（通过信号分发 OmniParser 识别出的步骤数据）